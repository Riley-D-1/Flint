function check_theme(){
    if (document.getElementById("").value = "space"){
        // Space theme saved to local storage
    }else if(document.getElementById("").value = "Light_theme"){
        // light theme saved to local storage
    }else{
        // No theme set yet or selected dark, set dark theme to local storage
    }
}